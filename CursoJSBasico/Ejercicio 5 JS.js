let altura = 184
let altMt= 1.84 
let peso = 84.5
let altRed= Math.ceil(altMt) 
let pesoRed= Math.floor(peso)
let maxValor = undefined

   if (Number.MAX_VALUE +1 === Number.MAX_VALUE) 
maxValor = true;
else maxValor = false;
console.log (maxValor)
