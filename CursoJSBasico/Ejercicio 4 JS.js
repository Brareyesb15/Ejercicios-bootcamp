"Brandon"
"Reyes"
let estudiante = "Brandon" + " " + "Reyes";
let estudianteMayus = estudiante.toUpperCase(); 
let estudianteMinus = estudiante.toLowerCase();
let numLetras= estudiante.length;
let primeraLet= estudiante[0];
let ultimaLet =  estudiante.charAt(estudiante.length-1);
let sinSpace = estudiante.replace(/ /g, "");


let booll = undefined
    if (estudiante.indexOf("Brandon") !== -1) 
        booll = true; 
        else booll = false
        
